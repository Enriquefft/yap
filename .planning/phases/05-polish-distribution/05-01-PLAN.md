---
phase: 05-polish-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/cmd/start.go
  - internal/config/config.go
  - internal/config/wizard.go
  - internal/config/wizard_test.go
autonomous: false
requirements:
  - FIRSTRUN-01
  - FIRSTRUN-02
  - FIRSTRUN-03
must_haves:
  truths:
    - "On first run (no config file), yap start launches interactive wizard"
    - "Wizard prompts for Groq API key, hotkey, and language"
    - "Wizard validates API key format before writing config"
    - "Wizard skips if GROQ_API_KEY env var is set"
    - "After wizard completes, config file exists and daemon starts"
  artifacts:
    - path: internal/config/wizard.go
      provides: "First-run wizard with interactive prompts"
      exports: ["RunWizard"]
    - path: internal/config/wizard_test.go
      provides: "Wizard test coverage"
    - path: internal/cmd/start.go
      provides: "Wizard invocation on first run"
  key_links:
    - from: internal/cmd/start.go
      to: internal/config/wizard.RunWizard
      via: "Config file existence check before daemon start"
      pattern: "os.Stat.*config.toml"
---

<objective>
Interactive first-run setup wizard that guides new users to a working configuration before starting the daemon.

Purpose: Eliminate friction for new users by providing guided setup when config file doesn't exist
Output: Working config file created, daemon starts successfully
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish-distribution/05-CONTEXT.md
@.planning/STATE.md

# Key interfaces from existing codebase

From internal/config/config.go:
```go
type Config struct {
	APIKey         string `toml:"api_key"`
	Hotkey         string `toml:"hotkey"`
	Language       string `toml:"language"`
	MicDevice      string `toml:"mic_device"`
	TimeoutSeconds int    `toml:"timeout_seconds"`
}

func Load() (Config, error)
func ConfigPath() (string, error)
```

From internal/cmd/start.go (to be modified):
```go
// newStartCmd creates the 'yap start' command
func newStartCmd(cfg *config.Config) *cobra.Command
```

Config file location: $XDG_CONFIG_HOME/yap/config.toml (via xdg.ConfigFile)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create wizard package with test coverage (RED)</name>
  <files>internal/config/wizard.go, internal/config/wizard_test.go</files>
  <behavior>
    - Test 1: RunWizard prompts for API key when config file doesn't exist
    - Test 2: RunWizard validates API key format (regex ^sk-[a-zA-Z0-9]{48}$)
    - Test 3: RunWizard prompts for hotkey with default "KEY_RIGHTCTRL"
    - Test 4: RunWizard prompts for language with default "en"
    - Test 5: RunWizard writes valid TOML config file to XDG path
    - Test 6: RunWizard returns error on invalid API key format (re-prompts)
    - Test 7: RunWizard is skipped when GROQ_API_KEY env var is set
    - Test 8: RunWizard confirms config file path after writing
  </behavior>
  <action>Create internal/config/wizard.go with RunWizard() function:
    - Check if GROQ_API_KEY env var is set → skip wizard if true
    - Print welcome message: "Welcome to yap! Let's set up your configuration..."
    - Prompt for API key with validation regex ^sk-[a-zA-Z0-9]{48}$
    - Prompt for hotkey with default "KEY_RIGHTCTRL"
    - Prompt for language with default "en"
    - Write config file atomically (write to temp file, then rename)
    - Confirm config path to user
    - Return Config struct with all values filled

    Use fmt.Scanln() for input (simple text-based prompts per CONTEXT.md decision)
  </action>
  <verify>
    <automated>go test -v ./internal/config/... -run TestRunWizard</automated>
  </verify>
  <done>All wizard tests passing; RunWizard() creates valid config file</done>
</task>

<task type="auto">
  <name>Task 2: Integrate wizard into yap start command</name>
  <files>internal/cmd/start.go</files>
  <action>Modify newStartCmd() in internal/cmd/start.go:
    - Before calling daemon.Start(), check if config file exists
    - If config file doesn't exist AND GROQ_API_KEY env var is not set:
      - Call wizard.RunWizard() to create config file
      - If wizard fails, return error and exit
      - Reload config after wizard completion (config.Load() to pick up new file)
    - If config file exists or GROQ_API_KEY is set, proceed to daemon start

    Add helper function: needsWizard() -> bool
    - Returns true if: config file missing AND GROQ_API_KEY env var not set

    Modify existing cmd.RunE to:
    1. Check if wizard is needed
    2. If yes, run wizard
    3. Reload config (config.Load())
    4. Proceed with daemon start
  </action>
  <verify>
    <automated>MANUAL — Run yap start on fresh system; wizard launches; config created; daemon starts</automated>
  </verify>
  <done>yap start triggers wizard on first run; daemon starts after wizard completes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human verification</name>
  <what-built>First-run wizard integrated into yap start command</what-built>
  <files>N/A - verification only</files>
  <action>N/A - human verification checkpoint</action>
  <verify>Human confirms functionality works correctly</verify>
  <done>Human approved checkpoint</done>
  <how-to-verify>
    1. Remove existing config file: rm ~/.config/yap/config.toml
    2. Unset GROQ_API_KEY: unset GROQ_API_KEY
    3. Run: yap start
    4. Verify wizard prompts appear (welcome message, API key prompt, hotkey prompt, language prompt)
    5. Enter valid Groq API key (format: sk-xxxxxxxxxxxxxxxx)
    6. Accept default hotkey (KEY_RIGHTCTRL) or enter custom
    7. Accept default language (en) or enter custom
    8. Verify confirmation message: "Config saved to ~/.config/yap/config.toml"
    9. Verify daemon starts (no error messages)
    10. Verify config file exists: cat ~/.config/yap/config.toml
    11. Test with env var: GROQ_API_KEY=sk-test yap start
    12. Verify wizard is skipped when env var is set
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Wizard launches when config file missing and GROQ_API_KEY not set
- API key validation rejects invalid formats (regex ^sk-[a-zA-Z0-9]{48}$)
- Wizard accepts valid API key and prompts for hotkey/language
- Config file written to correct XDG path
- Config file contains valid TOML with all required keys
- Daemon starts successfully after wizard completes
- Wizard is skipped when GROQ_API_KEY env var is set
</verification>

<success_criteria>
1. On fresh system with no config, `yap start` launches interactive wizard
2. Wizard validates API key format (regex ^sk-[a-zA-Z0-9]{48}$)
3. Wizard creates valid ~/.config/yap/config.toml with user-provided values
4. Daemon starts successfully after wizard completion
5. Wizard is skipped when GROQ_API_KEY env var is already set
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-distribution/05-01-SUMMARY.md`
</output>
