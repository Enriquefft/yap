---
phase: 02-audio-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/audio/audio_test.go
  - internal/audio/wav_test.go
  - internal/audio/chime_test.go
  - go.mod
  - go.sum
autonomous: true
requirements: [AUDIO-01, AUDIO-02, AUDIO-03, AUDIO-04, AUDIO-05, AUDIO-06, ASSETS-03, NFR-03, NFR-06]

must_haves:
  truths:
    - "go test ./internal/audio/... compiles and all stubs fail with t.Skip or TODO markers (not compilation errors)"
    - "go-audio/wav and go-audio/audio are in go.mod after go get"
    - "Test files exist for every requirement ID this phase must satisfy"
  artifacts:
    - path: "internal/audio/audio_test.go"
      provides: "Test stubs for AUDIO-01 through AUDIO-06, NFR-06"
      contains: "TestDeviceSelection, TestPipeWireGuard, TestNoTempFiles, TestRecorderFrames, TestEncodeWAV, TestInMemoryEncode"
    - path: "internal/audio/wav_test.go"
      provides: "Detailed WAV header validation stubs"
      contains: "TestWAVHeader, TestReadWriteSeeker"
    - path: "internal/audio/chime_test.go"
      provides: "Async chime test stubs for ASSETS-03, NFR-03"
      contains: "TestChimeAsync, TestChimeNonBlocking, BenchmarkRecorder"
  key_links:
    - from: "go.mod"
      to: "github.com/go-audio/wav"
      via: "go get github.com/go-audio/wav@v1.1.0"
      pattern: "go-audio/wav"
---

<objective>
Create Wave 0 test stubs for the audio pipeline and add go-audio/wav to go.mod.

Purpose: Nyquist rule — tests must exist before implementation. All subsequent plans run
tests as their verify step. Without this plan, Plans 02 and 03 have no automated verify
commands to run.

Output: Three test files with stubs for every requirement, and go.mod updated with
go-audio/wav@v1.1.0 and go-audio/audio (transitive).
</objective>

<execution_context>
@/home/hybridz/.claude/get-shit-done/workflows/execute-plan.md
@/home/hybridz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-pipeline/02-RESEARCH.md
@.planning/phases/02-audio-pipeline/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add go-audio/wav dependency and create audio package scaffold</name>
  <files>go.mod, go.sum, internal/audio/audio_test.go, internal/audio/wav_test.go, internal/audio/chime_test.go</files>
  <action>
Run:
  cd /home/hybridz/Projects/yap && go get github.com/go-audio/wav@v1.1.0

This pulls in github.com/go-audio/audio as a transitive dependency. Verify both appear in go.mod after the command.

Then create the three test stub files as package audio (these files define what the package will look like — they import the real package once implementation exists, so use build constraints and t.Skip stubs):

**internal/audio/audio_test.go** — package audio_test:
```go
package audio_test

import (
    "testing"
)

// TestDeviceSelection verifies AUDIO-01: OpenDefaultStream uses config.MicDevice when set,
// or system default when empty.
func TestDeviceSelection(t *testing.T) {
    t.Skip("Wave 0 stub — implement in Plan 02")
}

// TestPipeWireGuard verifies AUDIO-02: returns clear error when 0 input devices found
// after portaudio.Initialize().
func TestPipeWireGuard(t *testing.T) {
    t.Skip("Wave 0 stub — implement in Plan 02")
}

// TestNoTempFiles verifies AUDIO-03 + NFR-06: no files created in /tmp or
// $XDG_RUNTIME_DIR during a mock recording session.
func TestNoTempFiles(t *testing.T) {
    t.Skip("Wave 0 stub — implement in Plan 02")
}

// TestRecorderFrames verifies AUDIO-04: PCM data accumulates in []int16 slice,
// never via a Go channel inside the PortAudio callback.
func TestRecorderFrames(t *testing.T) {
    t.Skip("Wave 0 stub — implement in Plan 02")
}

// TestEncodeWAV verifies AUDIO-05: WAV bytes begin with "RIFF", are at least 44 bytes,
// and use 16kHz/16-bit/mono parameters.
func TestEncodeWAV(t *testing.T) {
    t.Skip("Wave 0 stub — implement in Plan 02")
}

// TestInMemoryEncode verifies AUDIO-06: encoding produces []byte with no file I/O;
// no os.File or os.CreateTemp used in the encode path.
func TestInMemoryEncode(t *testing.T) {
    t.Skip("Wave 0 stub — implement in Plan 02")
}
```

**internal/audio/wav_test.go** — package audio_test:
```go
package audio_test

import (
    "testing"
)

// TestWAVHeader verifies the RIFF/fmt/data chunk layout produced by encodeWAV().
// Checks: bytes 0-3 == "RIFF", bytes 8-11 == "WAVE", fmt chunk present, sample rate 16000.
func TestWAVHeader(t *testing.T) {
    t.Skip("Wave 0 stub — implement in Plan 02")
}

// TestReadWriteSeeker verifies the custom ReadWriteSeeker helper:
// Write appends bytes, Seek moves position, re-Write overwrites (not appends),
// Bytes() returns full accumulated buffer.
func TestReadWriteSeeker(t *testing.T) {
    t.Skip("Wave 0 stub — implement in Plan 02")
}

// TestInt16Conversion verifies that audio.IntBuffer.Data []int correctly stores
// int16 sample values without sign loss: int16(-32768) → int(-32768).
func TestInt16Conversion(t *testing.T) {
    t.Skip("Wave 0 stub — implement in Plan 02")
}
```

**internal/audio/chime_test.go** — package audio_test:
```go
package audio_test

import (
    "testing"
    "time"
)

// TestChimeAsync verifies ASSETS-03: PlayChime() returns within 5ms regardless
// of chime duration — the goroutine runs independently.
func TestChimeAsync(t *testing.T) {
    t.Skip("Wave 0 stub — implement in Plan 03")
}

// TestChimeNonBlocking verifies AUDIO-06 (chime variant): starting a chime while
// a recording mock is active does not introduce timing delay to the recorder.
func TestChimeNonBlocking(t *testing.T) {
    t.Skip("Wave 0 stub — implement in Plan 03")
    _ = time.Millisecond // keep import used
}

// BenchmarkRecorder verifies NFR-03: memory allocated during mock encode of 60s
// audio must not exceed 15MB.
func BenchmarkRecorder(b *testing.B) {
    b.Skip("Wave 0 stub — implement in Plan 03")
}
```

After creating all three files, run `go build ./...` to confirm no compile errors.
  </action>
  <verify>
    <automated>cd /home/hybridz/Projects/yap && go test ./internal/audio/... 2>&1 | grep -E "SKIP|ok|no test files" && grep "go-audio/wav" go.mod && grep "go-audio/audio" go.mod</automated>
  </verify>
  <done>
- go.mod contains github.com/go-audio/wav v1.1.0 and github.com/go-audio/audio
- All three test files compile without errors
- `go test ./internal/audio/...` outputs SKIP lines (not build errors or panics)
- `go build ./...` passes
  </done>
</task>

</tasks>

<verification>
go test ./internal/audio/... — all stubs SKIP, no build failures
go build ./... — full build green
grep go-audio/wav go.mod — dependency present
</verification>

<success_criteria>
Wave 0 complete: three test files exist with named stubs for every requirement, go-audio/wav is in go.mod, and the package compiles. Plans 02 and 03 can now use `go test ./internal/audio/...` as their verify command.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-pipeline/02-01-SUMMARY.md`
</output>
